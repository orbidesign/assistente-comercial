{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/node",
      "config": {
        "installCommand": "npm install --force && npm install react-scripts --save"
      }
    }
  ]
}
